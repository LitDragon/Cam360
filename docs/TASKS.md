# TASKS

本文件记录当前任务、下一步计划和待决事项。每次 AI 完成一轮实际改动后，都应同步更新。

## 当前任务

1. 当前无真设备和硬件联调条件，暂停依赖端到端设备验证的接入任务。
2. 现阶段只基于现有 UI 和 `/Users/naxclow/camera-360-secives` 的连接、协议、UI 文档推进，不把外部资料当作已验证硬件行为。
3. 继续完善可离线确认的 UI、onboarding/AP 连接流程边界和 `DeviceSession` 契约；离线 feature 页面已可从主流程进入，文件/回放/缩略图/截图/录像/危险操作已到命令级接入，真实预览、下载任务、本地保存和设置写操作等端到端行为待硬件恢复后再接。

## 下一步计划

1. 继续为协议解析、状态转换和会话契约补离线测试；不新增依赖真实设备响应的运行时代码路径。
2. 保持离线 feature 页面的真实设备依赖为占位；如继续推进，只补禁用态、错误态和入口说明。
3. 将 `DeviceProtocolEndpoint` 自动发现、真实热点连接、普通设置写操作、危险操作 UI 触发、主动推送状态消费、预览流、播放器、本地保存和下载任务保留为硬件恢复后的联调队列。

## 完成记录

- `2026-05-08`：补 `DeviceSession` 协议错误码映射和离线测试；未知 `errno` 继续保留原始错误码和 Topic。
- `2026-05-08`：补 onboarding/AP 的系统 Wi-Fi 手动接入指引、本地网络权限说明和失败恢复文案；仍不接真实热点自动连接。
- `2026-05-08`：补 `LivePreviewStore` 和 `EventsStore` 的离线状态、刷新反馈、禁用操作说明和事件筛选；仍不接真实视频流、事件推送或播放器。

## 待决事项

- 是否需要恢复独立的 UI 冒烟测试 target。
- 真设备 `DeviceProtocolEndpoint` 自动发现规则需要硬件或固件侧确认。
- Settings 的 Privacy Policy、Terms of Service 和真实联系方式仍缺业务正文或真实联系方式，只能保持占位。
- 截图、录像命令模型已接入；页面按钮接线、本地资源保存和真实预览画面仍需真设备或可信设备端模拟器确认。
- 下载任务、播放器、普通设置写操作、危险操作 UI 触发和主动推送状态消费仍需真设备或可信设备端模拟器确认。

## 更新规则

- 做完一轮改动后：
  - 把新的短期目标写回“当前任务”或“下一步计划”
  - “完成记录”只保留 3 条结果记录，不复述 `CHANGELOG.md`
  - 不记录编译、测试等直观验证信息
  - 如果有用户新指令，优先覆盖旧计划
  - 文档内容保持精简，不重复复述长期事实
